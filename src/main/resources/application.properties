spring.application.name=AI-Log-Debugger-1
spring.port=8080

# Load secure secrets file if it exists
spring.config.import=optional:classpath:application-secrets.properties

# Redis Configuration
spring.redis.host=localhost
spring.redis.port=6379